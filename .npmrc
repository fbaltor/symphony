# Prevent pnpm from walking up to a parent workspace if this repo is nested
# inside a monorepo. Safe to keep in standalone checkouts.
ignore-workspace=true

# Strict peer dependencies are noisy and block installs; warn only.
strict-peer-dependencies=false
